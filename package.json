{
  "name": "shipway",
  "version": "1.0.0",
  "private": true,
  "description": "Shipway — a fast, native-process deployment dashboard",
  "workspaces": [
    "server",
    "web"
  ],
  "scripts": {
    "test": "npm run test -w server",
    "typecheck": "npm run typecheck -w server && npm run typecheck -w web",
    "build": "npm run build -w server && npm run build -w web"
  },
  "license": "ISC"
}
